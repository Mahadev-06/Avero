services:
  - type: web
    name: avero-backend
    env: docker
    dockerfilePath: backend/Dockerfile
    dockerContext: backend
    plan: free
    healthCheckPath: /api/v1/health/
    envVars:
      - key: PORT
        value: 8000
      - key: ENVIRONMENT
        value: production
